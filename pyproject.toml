[project]
name = "langchain-k8s"
version = "0.1.0"
description = "Lightweight LangChain tool server with Kubernetes scale+wait"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
	"fastapi>=0.110.0",
	"ipykernel>=7.2.0",
	"ipywidgets>=8.1.8",
	"kubernetes",
	"uvicorn>=0.42.0",
]
